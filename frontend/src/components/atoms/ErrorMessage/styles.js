const styles = theme => ({
    container: {
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        marginTop: theme.spacing(4),
    },
    message: {
        color: theme.palette.error.main,
    },
});

export default styles;
